# The Chigwell Marquees — environment template.
# Page 00 runs entirely on typed seed data + stubs; nothing below is required to boot.
# Fill these in as the corresponding integrations are wired (post Page 00).

# Site
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# CMS — Payload v3 + Postgres (swap-in replacement for the seed cms client)
DATABASE_URL=""
PAYLOAD_SECRET=""

# Email — Resend (enquiry notify + auto-reply + brochure delivery)
RESEND_API_KEY=""
ENQUIRY_FROM_EMAIL="hello@thechigwellmarquees.com"

# Spam — Cloudflare Turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# Media — Mux (adaptive hero video)
MUX_TOKEN_ID=""
MUX_TOKEN_SECRET=""

# Maps — Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=""
